# Design Guidelines — Mineral Bridge Dashboard\n+\n+This document captures the design system decisions applied across the dashboard codebase.\n+\n+Summary (canonical rules)\n+\n+- Spacing\n+  - 8px baseline grid. Prefer margins/padding using the scale: 4 / 8 / 12 / 16 / 24 / 32 / 40 / 48.\n+\n+- Typography\n+  - H1: 24px\n+  - H2: 20px\n+  - H3: 16px\n+  - Body: 14px\n+  - Small / Meta: 12px\n+\n+- Colors & tokens\n+  - Primary / accent: emerald (use existing --color-emerald-* tokens in `index.css`).\n+  - Success, warning, danger: use `--color-emerald-*`, `--color-amber-*`, `--color-red-*` tokens.\n+\n+- Buttons\n+  - Primary: filled emerald (use `Button` component default variant where appropriate).\n+  - Secondary: outline variant.\n+  - Ghost / icon: use `variant=\"ghost\"` or `size=\"icon\"`.\n+\n+- Cards\n+  - Use `Card` primitive with `CardHeader` (`CardTitle` + `CardDescription`) and `CardContent`.\n+  - Visual: `border-none shadow-sm rounded-lg`.\n+\n+- Dialogs & Alerts\n+  - Dialogs and AlertDialogs are **centered, compact modals** by default: `sm:max-w-md` (~448px) and `max-width: calc(100vw - 2rem)` on narrow viewports.\n+  - Use `DialogHeader`, `DialogTitle`, `DialogDescription`, and `DialogFooter` consistently.\n+  - Provide a single primary action on the right in the footer.\n+\n+- Sheets / Drawers\n+  - Side panels (sheets) remain wider: `sm:max-w-[800px]` (intended as side panel UX) so keep `SheetContent` defaults intact.\n+\n+- Forms & inputs\n+  - Labels on top, compact vertical spacing. Primary actions on the right side in footers.\n+\n+- Tables\n+  - Compact row heights, header bold, actions right-aligned.\n+\n+- Empty states\n+  - Use dashed card with icon, short copy and single CTA.\n+\n+- Flow & steps\n+  - Steps are driven by `order.type` and `order.status`. Steps may include per-step metadata (e.g., `flowStepData.priceConfirmed`) and have dedicated step dialogs for admin input.\n+\n+- Accessibility\n+  - Dialogs trap focus, close on Escape, and have accessible labels (ensure `DialogTitle` is present).\n+\n+Implementation notes\n+\n+- Core changes applied\n+  - `src/components/ui/dialog.tsx` — default DialogContent standardized to compact modal (`sm:max-w-md`).\n+  - `src/components/ui/alert-dialog.tsx` — Alert dialogs use compact modal style.\n+  - Many dialog usages updated to not override the compact default (replaced `max-w-[800px]` with `sm:max-w-md`).\n+  - Flow & status: `OrderDetailPage` supports step-specific dialogs (Price Confirmed, Payment Initiated, Order Completed) and stores per-step data in `order.flowStepData`.\n+\n+- Files updated (representative)\n+  - `src/components/ui/dialog.tsx`\n+  - `src/components/ui/alert-dialog.tsx`\n+  - `src/components/admin/OrderDetailPage.tsx` (flow dialogs + compact sizes)\n+  - `src/components/admin/OrderTransactionManagement.tsx` (status dialog compact + flow sync)\n+  - Multiple other dialog instances were updated to follow `sm:max-w-md` (see git diff / PR for full list).\n+\n+Next steps / cleanup (recommended)\n+\n+1. Replace remaining layout containers that use `max-w-[800px]` when they are dialogs (we converted dialog uses; sheets remain as side panels).\n+2. Create CSS tokens file (optional) to map sizes and spacing to named variables for easier maintenance.\n+3. Run a visual QA pass acrossbreakpoints and a11y checks (contrast + focus).\n+4. Document component usage patterns in examples and add a short checklist for PR reviewers.\n+\n+If you want I will (next):\n+- finish any remaining dialog replacements, and\n+- add a short `DESIGN_TOKENS.md` with token names and quick copy/paste tokens to use in components.\n+\n*** End Patch"}**endphp.Logging code/plain**adzirisa**}()
